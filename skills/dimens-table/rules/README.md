# rules（发布兼容层）

本目录用于兼容 ClawHub / OpenClaw 的 `rules/` 目录扫描约定。

当前技能的正文仍以同目录技能包为准，推荐阅读顺序：

1. 先看当前技能目录下的 `SKILL.md`
2. 再看当前技能目录下的 `README.md`
3. 需要字段设计、建表流程和接口级细节时，再看当前技能目录下的 `references/`

如果发布平台不支持相对链接或不保留原始目录跳转，直接按上面的目录顺序在当前技能包内浏览即可。
